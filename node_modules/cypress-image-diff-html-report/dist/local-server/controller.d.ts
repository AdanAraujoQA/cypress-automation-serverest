import { ResolvedUserConfig, ResolvedReport, TestIdentity } from '../common/types.js';
export declare class Controller {
    private config;
    constructor(config: ResolvedUserConfig);
    getReports(): Promise<ResolvedReport>;
    updateTest(testId: TestIdentity): Promise<void>;
    private findTest;
    private updateScreenshots;
}
